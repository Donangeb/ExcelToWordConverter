<Window x:Class="ExcelToWordConverter.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        xmlns:local="clr-namespace:ExcelToWordConverter.Converters"
        Title="Excel → Word Converter" 
        Width="700" 
        Height="500"
        WindowStartupLocation="CenterScreen" 
        Background="#FAFAFA"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- Конвертер BooleanToVisibility -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <local:FileNameConverter x:Key="FileNameConverter"/>
        
        <!-- Эффект тени -->
        <DropShadowEffect x:Key="ShadowEffect" 
                         Color="Black" 
                         Direction="270" 
                         ShadowDepth="3" 
                         BlurRadius="10" 
                         Opacity="0.2"/>
        
        <!-- Стиль для скругленных кнопок -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect" Value="{StaticResource ShadowEffect}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" 
                                CornerRadius="8" 
                                Background="{TemplateBinding Background}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#1565C0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#0D47A1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для карточек с тенями -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" 
                                    Direction="270" 
                                    ShadowDepth="2" 
                                    BlurRadius="12" 
                                    Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и год обучения -->
        <Border Grid.Row="0" 
                Style="{StaticResource CardStyle}" 
                Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" 
                       VerticalAlignment="Center">
                <TextBlock Text="Год обучения:" 
                          FontSize="14" 
                          FontWeight="SemiBold"
                          VerticalAlignment="Center" 
                          Margin="0,0,12,0"/>
                <TextBox Width="120" 
                         Height="32"
                         Padding="8,4"
                         FontSize="13"
                         BorderThickness="1"
                         BorderBrush="#DDD"
                         VerticalContentAlignment="Center"
                         Text="{Binding CurrentYear, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </Border>

        <!-- Панель кнопок управления -->
        <Border Grid.Row="1" 
                Style="{StaticResource CardStyle}" 
                Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource RoundedButton}" 
                        Command="{Binding SelectFilesCommand}">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconImage Icon="FileExcel"  
                                       Width="16" 
                                       Height="16" 
                                       Margin="0,0,8,0"
                                       Foreground="White"/>
                        <TextBlock Text="Выбрать файлы Excel" 
                                  VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <Button Style="{StaticResource RoundedButton}" 
                        Command="{Binding ClearFilesCommand}" 
                        Margin="12,0,0,0"
                        Background="#6C757D">
                    <StackPanel Orientation="Horizontal">
                        <fa:IconImage Icon="Times"                                       
                                      Width="16" 
                                      Height="16" 
                                      Margin="0,0,8,0"
                                      Foreground="White"/>
                        <TextBlock Text="Очистить список" 
                                  VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Список файлов -->
        <Border Grid.Row="2" 
                Style="{StaticResource CardStyle}">
            <ListBox ItemsSource="{Binding Files}" 
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="4"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F8F9FA" 
                                CornerRadius="4" 
                                Padding="12,8" 
                                Margin="0,2">
                            <TextBlock Text="{Binding Path=., Converter={StaticResource FileNameConverter}}"
                                      FontSize="13"
                                      VerticalAlignment="Center"/>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Панель статуса и конвертации -->
        <Border Grid.Row="3" 
                Style="{StaticResource CardStyle}" 
                Margin="0,12,0,0"
                Background="Transparent"
                BorderThickness="0"
                Padding="0">
            <StackPanel Orientation="Horizontal" 
                       HorizontalAlignment="Right">
                <TextBlock Text="{Binding Status}" 
                          VerticalAlignment="Center" 
                          Foreground="#6C757D"
                          FontSize="13"
                          Margin="0,0,16,0"/>
                
                <ProgressBar Width="160" 
                            Height="8"
                            IsIndeterminate="True"
                            Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                            Margin="0,0,16,0">
                    <ProgressBar.Resources>
                        <Style TargetType="ProgressBar">
                            <Setter Property="Background" Value="#E9ECEF"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsIndeterminate" Value="True">
                                    <Setter Property="Foreground" Value="#1976D2"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ProgressBar.Resources>
                </ProgressBar>

                <Button Style="{StaticResource RoundedButton}"  x:Name="ConvertButton"
                        Command="{Binding ConvertAsyncCommand}"
                        Background="#28A745">
                    <StackPanel Orientation="Horizontal" >
                        <fa:IconImage Icon="Sync" 
                                       Width="16" 
                                       Height="16" 
                                       Margin="0,0,8,0"
                                       Foreground="White"/>
                        <TextBlock Text="Конвертировать" 
                                  VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>